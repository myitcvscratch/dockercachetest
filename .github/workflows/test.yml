on: [push]
name: Test
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
      - name: Install Go
        uses: actions/setup-go@v4
        with:
          cache: false
          go-version: "1.20.4"
      - name: Print go cache paths
        run: go env
      - name: Cache setup
        uses: actions/cache@v3
        with:
          path: |-
            /path/to/dockercache
          key: ${{ runner.os }}
          restore-keys: ${{ runner.os }}
      - name: Run docker
        run: |-
          docker buildx build -t preprocessor --load -f ./Dockerfile .
